{
  "name": "declare-system",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "backend": "json-server --watch data/db.json --routes data/routes.json --port 3000"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "json-server": "^0.17.1"
  }
}
